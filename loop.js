
let number = [25, 22, 65, 90, 44, 80, 55]
for (i = 0; i <= number.length-1; i++){
    if (number[i] < 70){
        console.log(number[i] + 20);
    }
    else if(number[i] >= 80 && number[i] < 90){
        console.log(number[i] + 5);
    }
    else{
         console.log(number[i]);
    }
}

/* for each variable 20 is added to values below 70 & 5 is added to values queal to 80 & less than 90. All the latest values are printed */

/* for each variable 20 is added to values below 70 & 5 is added to values queal to 80 & less than 90. All the latest values are printed */
